"""J.A.R.V.I.S. — an Iron Man inspired personal voice assistant.

Importing this package is side-effect free: all heavy third-party
dependencies (audio, speech, vision, ...) are imported lazily inside the
functions that need them, so the package can be imported and tested in a
headless environment without those optional dependencies installed.
"""

__version__ = "0.2.0"
